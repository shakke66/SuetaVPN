import { AppShell } from 'suetavpn';

/**
 * Оболочка кабинета: шапка с навигацией и утилитами, область маршрута,
 * нижняя навигация на мобильной ширине.
 *
 * Содержимое страницы приходит через маршрут, поэтому в карточке область
 * под шапкой пустая — здесь показана именно рама, в которую эти страницы
 * вставляются.
 */
export const Shell = () => <AppShell />;
